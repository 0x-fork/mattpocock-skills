# Credits

Step 5 (drawing the shape of the change) is [Dex Horthy](https://github.com/dexhorthy)'s [`show-me`](https://github.com/humanlayer/humanlayer) technique, aimed at a diff instead of a live conversation: the smallest visual that makes the point, embedded directly rather than shipped as a standalone artifact. `pr` does not depend on `show-me` as a skill (it isn't part of this repo, and inlining the technique keeps `pr` installable on its own); this file is the attribution that dependency would otherwise have carried.

The domain-vocabulary step (3) follows the same "read the glossary, flag ADR conflicts" discipline `grill-with-docs`/`domain-modeling` document in [`docs/agents/domain.md`](https://github.com/mattpocock/personal-wiki/blob/main/docs/agents/domain.md)-style files: consuming the glossary, not building it, so `pr` states the habit inline rather than calling either skill.
